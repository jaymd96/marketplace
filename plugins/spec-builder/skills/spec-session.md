---
name: spec-session
description: "Build a product specification through conversation. Use when the user wants to define a product, build a spec, scope out a system, write requirements, or create a technical specification."
---

# spec session

Read `system/ENTRYPOINT.md` in this plugin's directory and follow the session
start protocol. The entrypoint will:

1. Check if a project exists or needs bootstrapping
2. Orient you on the current state
3. Route you to the right stance based on what the human is doing

See ENTRYPOINT.md for the journey map, stance routing, and session protocol.
